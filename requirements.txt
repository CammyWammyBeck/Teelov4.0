# Web Server Dependencies (Lightweight)
# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Frontend / Content
jinja2>=3.1.0
markdown>=3.5.0
python-frontmatter>=1.0.0

# Utilities
python-dotenv>=1.0.0
structlog>=24.1.0
tenacity>=8.2.0

# Required by SQLAlchemy/Models even if we don't use full features
# (These are small enough to keep)
greenlet>=3.0.0
typing-extensions>=4.8.0
